# Generated scenarios

Run `generate_dynamic_scenarios.py` to create Arena-tools-compatible JSON in
`data/dynamic_eval/scenarios`. Generated files are kept outside the ROS package
so large benchmark sets do not become package resources.
